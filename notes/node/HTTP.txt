
*) HTTP
  # Request
    > An HTTP request message
  # Response
    > An HTTP request message
  # Resource
    > A network data object or service which can be identified by an URI
  # Entity
    > A particular representation or rendition of a data